<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Title</th>
      <th>Description</th>
      <th>Date</th>
      <th>Author</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.image %></td>
        <td><%= post.title %></td>
        <td><%= post.description %></td>
        <td><%= post.date %></td>
        <td><%= post.author %></td>
        <td><%= link_to 'Show', post %></td>
       
      </tr>
    <% end %>
  </tbody>
</table>

<br>


